---
- name: Test grep
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Test for line
      lineinfile: 
         path: /home/ysurmd/ansible/module_test/test.txt
         line: b
         regexp: '^q'
      ignore_errors: yes
      register: test_lineinfile

    - name: Debug test_grep
      debug:
        msg: "{{ test_lineinfile }}"
